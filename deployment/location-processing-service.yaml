apiVersion: v1
kind: Service
metadata:
  name: location-processing
spec:
  type: ClusterIP
  selector:
    app: location-processing
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
